---
# Coleções Ansible necessárias. Instale com:
#   ansible-galaxy collection install -r ansible/requirements.yml
collections:
  - name: community.zabbix
    version: ">=3.0.0"
  - name: community.grafana
    version: ">=1.9.0"
  - name: community.general
    version: ">=8.0.0"
  - name: community.mysql
    version: ">=3.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
